Propose a clean solution structure for a C#/.NET app that:
- uses forms authentication
- integrates with SSRS 2019
- has a web UI + a small API layer
- includes unit tests + integration tests
- supports app settings per environment

Output:
- folder layout
- project list (Web, Application, Domain, Infrastructure, Tests)
- naming conventions
- initial NuGet package recommendations (minimal set)
Ask whether we prefer Clean Architecture vs simpler layered architecture.