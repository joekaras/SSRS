Create a local development checklist for:
- Visual Studio 2022 and VS Code usage split
- SQL Server local setup (or Docker if allowed)
- SSRS 2019 connectivity assumptions
- secrets (user-secrets, environment variables)
- debugging auth cookies and SSRS calls

Make it step-by-step and include common failure modes.